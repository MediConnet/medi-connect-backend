-- Drop tables supply_orders and supply_order_items (not needed)
-- These tables were created for an e-commerce feature that is not being used
-- The supplies section only shows products, no purchasing functionality

DROP TABLE IF EXISTS supply_order_items CASCADE;
DROP TABLE IF EXISTS supply_orders CASCADE;
